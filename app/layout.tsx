import "./globals.css";
import Header from "./components/ButtonLink/Header";
import Footer from "./components/ButtonLink/Footer";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ja">
      <body>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
